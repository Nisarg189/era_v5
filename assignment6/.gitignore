# Regenerated by run_demo.py on every run.
data/

# Model weights are regenerated too.  submission_artifacts/checkpoints/index.json is
# committed instead: it names every checkpoint, the ledger offset it is bound to and
# a digest of its weights.
submission_artifacts/checkpoints/*.pt

__pycache__/
*.pyc
.DS_Store
